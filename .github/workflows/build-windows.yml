name: Build Windows Executable

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Cho phép chạy thủ công từ GitHub UI

jobs:
  build:
    runs-on: windows-latest  # Chạy trên Windows
    env:
      PYTHONIOENCODING: utf-8  # Fix encoding for Vietnamese and emojis

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🐍 Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🔨 Build Windows executable
      run: |
        echo "🔨 Building Windows .exe..."
        pyinstaller --name=MusicScheduler --onefile --windowed --clean music_scheduler_gui.py

        echo "📦 Creating distribution package..."
        mkdir MusicScheduler-Windows
        copy dist\MusicScheduler.exe MusicScheduler-Windows\
        echo 🎵 Music Scheduler - Windows Edition > MusicScheduler-Windows\README.txt
        echo. >> MusicScheduler-Windows\README.txt
        echo Cach su dung: >> MusicScheduler-Windows\README.txt
        echo 1. Chay MusicScheduler.exe >> MusicScheduler-Windows\README.txt
        echo 2. Chon folder nhac >> MusicScheduler-Windows\README.txt
        echo 3. Them lich phat nhac >> MusicScheduler-Windows\README.txt
        echo 4. Nhan "Bat Dau" >> MusicScheduler-Windows\README.txt

        powershell Compress-Archive -Path MusicScheduler-Windows -DestinationPath MusicScheduler-Windows.zip

    - name: 📤 Upload Windows app artifact
      uses: actions/upload-artifact@v4
      with:
        name: MusicScheduler-Windows
        path: MusicScheduler-Windows.zip
        retention-days: 30

    - name: ✅ Build complete
      run: |
        echo "✅ Build Windows thành công!"
        echo "📦 File đã được tạo: MusicScheduler.exe"
